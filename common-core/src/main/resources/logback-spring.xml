<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Load Spring Boot defaults -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Console Appender (STDOUT) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Elasticsearch Appender -->
    <appender name="ELASTICSEARCH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>${ELASTICSEARCH_HOST:-elasticsearch}:${ELASTICSEARCH_LOGSTASH_PORT:-5000}</destination>

        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeContext>true</includeContext>
            <includeMdc>true</includeMdc>
            <includeCallerData>true</includeCallerData>

            <!-- Add custom fields -->
            <customFields>{"app_name":"${spring.application.name:-unknown}", "environment":"${SPRING_PROFILES_ACTIVE:-dev}"}</customFields>
        </encoder>

        <!-- Connection settings -->
        <keepAliveDuration>5 minutes</keepAliveDuration>
        <reconnectionDelay>10 seconds</reconnectionDelay>
        <writeBufferSize>16384</writeBufferSize>
    </appender>

    <!-- Async wrapper for Elasticsearch (non-blocking) -->
    <appender name="ASYNC_ELASTICSEARCH" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <appender-ref ref="ELASTICSEARCH"/>
    </appender>

    <!-- Root logger -->
    <root level="${LOG_LEVEL_ROOT:-INFO}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_ELASTICSEARCH"/>
    </root>

    <!-- Application loggers -->
    <logger name="com.example.cleanarch" level="${LOG_LEVEL_APP:-DEBUG}"/>
    <logger name="org.hibernate.SQL" level="${LOG_LEVEL_SQL:-DEBUG}"/>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="${LOG_LEVEL_SQL_PARAMS:-TRACE}"/>
</configuration>
